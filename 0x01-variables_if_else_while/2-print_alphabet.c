#include <stdio.h>

/**
 * main - prints alphabet in lower case
 * Return: Always 0
 */
int main(void)
{
	char hj;

	for (hj = 'a'; hj <= 'z'; hj++)
	{
		putchar(hj);
	}
	putchar('\n');
	return (0);
}
